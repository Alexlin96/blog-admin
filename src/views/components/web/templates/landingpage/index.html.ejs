<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= tmpConfigs.title %></title>
  <link rel="shortcut icon" href="https://www.goatgames.com/assets/images/favicon.ico" type="image/x-icon">
  <meta name="keywords" content="<%= tmpConfigs.keywords %>">
  <meta name="description" content="<%= tmpConfigs.description %>">
  <style>
    html,
    body {
      width: 100%;
      height: 100%;
      margin: 0;
      padding: 0
    }

    body {
      font: 12px, "宋体", "Arial Narrow", HELVETICA;
      background: #17191f
    }

    ol,
    ul,
    h1,
    h2,
    h3,
    h4,
    h5,
    h6,
    p,
    th,
    td,
    dl,
    dd,
    form,
    fieldset,
    legend,
    input,
    textarea,
    select {
      margin: 0;
      padding: 0
    }

    a {
      color: #2d374b;
      text-decoration: none
    }

    a:hover {
      cursor: pointer
    }

    em {
      font-style: normal
    }

    li {
      list-style: none
    }

    img {
      max-width: 100%;
      border: 0;
      vertical-align: middle
    }

    table {
      border-collapse: collapse;
      border-spacing: 0
    }

    p {
      word-wrap: break-word
    }

    .floor-page {
      position: relative;
      margin: 0 auto;
      max-width: 720px;
    }

    .floor-page .top {
      position: sticky;
      top: 0;
      left: 0;
      z-index: 999;
    }

    .floor-page .top img {
      display: block
    }

    .fixed-footer {
      padding: 10px;
      color: #727272;
      background: #ffffff;
      font-size: 12px;
      text-align: center;
      line-height: 1.6;
    }

    .fixed-footer a {
      color: #727272;
      text-decoration: underline;
    }
  </style>
</head>

<body>
  <div id="floor-page" class="floor-page">
    <div class="fixed-footer">
      开发者：<%= tmpConfigs.developer %> | 应用名称：<%= tmpConfigs.appName %><br />版本号：<%= tmpConfigs.version %> | 更新时间：<%= tmpConfigs.updateTime %> | <a
        href="<%= tmpConfigs.privacy %>" target="_blank">隐私政策</a> | <a
        href="<%= tmpConfigs.authority %>" target="_blank">应用权限</a></div>
    <div class="top j-download"><img src="<%= tmpConfigs.jDownload %>" alt="failed to load"></div>
    <div class="content">
      <div class="images"><img src="<%= tmpConfigs.img_1 %>" alt="failed to load">
        <div class="in-btn j-download">
          <img src="<%= tmpConfigs.jDownloadBtn %>" alt="failed to load">
        </div>
      </div>
      <div class="images"><img src="<%= tmpConfigs.img_2 %>" alt="failed to load"></div>
      <div class="images"><img src="<%= tmpConfigs.img_3 %>" alt="failed to load"></div>
    </div>
    <div class="footer j-download"><img src="<%= tmpConfigs.img_5 %>" alt="failed to load"></div>
  </div>
  <script>
    var downUrl = 'https://apk-download.goatgames.com';
    var path = location.pathname.replace('/index.html', '');
    var p = path;
    var filename = path.substr(1, path.length).replace(/\//g, '_');
    downUrl += (p + '/' + filename + '.apk');

    function downloadGame() {
      window.location.href = downUrl
    }
    var downELs = document.querySelectorAll('.j-download')
    for (var i = 0; i < downELs.length; i++) {
      downELs[i].onclick = function (e) {
        if (filename === '') return;
        downloadGame()
      }

    }
  </script>
</body>

</html>